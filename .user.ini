; PHP-лимиты для FPM/CGI (аналог php_value из .htaccess, который ломает FPM).
; Если хостинг игнорирует .user.ini — задайте те же значения в панели → «Настройки PHP».
upload_max_filesize = 20M
post_max_size = 20M
max_execution_time = 300
max_input_time = 300
