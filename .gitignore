# Логи
logs/
*.log
debug-*.log

# Rate limiting кэш
.rate_limit/

# Кэш приложения
cache/

# Загруженные файлы
uploads/*
!uploads/.gitkeep
!uploads/.htaccess
!uploads/photos/
uploads/photos/*
!uploads/photos/.htaccess
!uploads/scans/
uploads/scans/*
!uploads/scans/.htaccess
api/uploads/

# Временные каталоги хостинга
tmp/

# IDE
.vscode/
.idea/
.cursor/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local test / caches / scratch DBs
cookies.txt
database.sqlite
database_sqlite.sql

# PHP / конфигурация
.env
.env.local
.env*.local
composer.lock
vendor/
.phpunit.cache/

# Node (если используется)
node_modules/
package-lock.json

# Временные файлы
*.tmp
*.temp

# Verity: churn-heavy run log (per verity-setup it should not be committed)
.verity/memory/log.md
