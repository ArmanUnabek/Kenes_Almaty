# Деплой на zhurnal.zhasylumit.kz — 13.07.2026

## 1. Залить файлы
Залить содержимое архива `deploy_20260713.zip` (или весь проект) в корень сайта.

**НЕ заливать:** `node_modules/`, `composer.phar`, `.claude/`, `logs/`, `DEPLOY.md`.

**Удалить с прода:** `test.php`.

## 2. База данных — ОДИН файл (БД была удалена → чистая установка)
```bash
mysql -u USER -p DBNAME < migrations/INSTALL_FULL_2026_07_13.sql
```
или phpMyAdmin → выбрать БД → «Импорт» → этот файл. Внутри: все 41 таблица + правильные FULLTEXT-индексы + сид-данные.

**Первый вход: логин `admin`, пароль `admin123` — СМЕНИТЬ сразу после входа** (также сменить у `moderator`/`viewer`).

⚠️ Только на ПУСТУЮ базу. Для будущих обновлений существующей БД — `migrations/DEPLOY_2026_07_13_all.sql`.

## 3. Секреты (скомпрометированы — менять обязательно)
- [ ] Пароль БД → обновить в конфиге
- [ ] Пароль SMTP → обновить в конфиге
- [ ] Токен Telegram-бота → перевыпустить у BotFather, обновить env + вебхук

## 4. Crontab
```cron
0 3 * * *   /bin/bash /path/to/scripts/backup_db.sh    # бэкап БД, ротация 14 дней
0 9 * * *   php /path/to/cron_deadline_reminders.php   # TG-сводки о дедлайнах
*/5 * * * * php /path/to/cron_send_emails.php          # email-очередь (если не настроено)
```

## 5. Проверка после деплоя
- [ ] `php tests/smoke.php https://zhurnal.zhasylumit.kz` — все проверки зелёные
- [ ] Вход, календарь, «Активные сессии», 2FA (QR рисуется)
- [ ] Новое: дашборд (воронка, теплокарта, PNG-экспорт), массовые операции, колокольчик (лента), шаблоны с переменными, поиск, экспорт-центр, баннер «Установить приложение»
- [ ] Офлайн-режим (SW-кэши v13); при проблемах с кэшем — Ctrl+F5
- [ ] Когда все админы включат 2FA → `ENFORCE_ADMIN_2FA=true`

## Текущие версии
`dist/app.js?v=37` · SW-кэши v13 · `styles.css?v=34`
